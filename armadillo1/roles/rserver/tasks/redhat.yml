---
- name: dockerhub ratelimit prevention | Installation (Redhat)
  command: cat {{ dockerhub.password }} | podman login --username {{ dockerhub.username }} --password-stdin
  when: dockerhub.enabled

- name: pull the image | Installation (Redhat)
  command: podman pull {{ image.repo }}/{{ image.name }}:{{ image.version }}