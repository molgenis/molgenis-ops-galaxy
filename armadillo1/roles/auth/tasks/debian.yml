---
- name: dockerhub ratelimit prevention | Installation
  ansible.builtin.shell:
    cmd: cat {{ dockerhub.password }} | docker login --username {{ dockerhub.username }} --password-stdin
  when: dockerhub.enabled

- name: pull the image | Installation
  ansible.builtin.shell:
    cmd: docker pull {{ image.repo }}/{{ image.name }}:{{ image.version }}